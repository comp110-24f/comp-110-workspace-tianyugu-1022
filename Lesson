"""Practice with functions."""


# Define a function
def sum(num1: int, num2: int) -> int:
    """Return num1 + num2."""
    return num1 + num2


# Call the function
print(sum(num1=11, num2=3))  # <- 11 and 3 are arguments

# sum (num1=randint(1,10), num2=randint(2,20))
